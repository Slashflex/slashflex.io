{% extends 'base.html.twig' %}

{% block title %}{{ title }}
{% endblock %}


{% block body %}
	<!-- Logo and Nav -->
	{% include "partials/_top_buttons.html.twig" %}

	<!-- Fullscreen Navigation -->
	{% include "partials/_fullscreen_nav.html.twig" %}

	{# TEST #}
	<header class="header__projects">
        <div class="hero__projects">
            <img src="/build/images/sites_laptop.png" class="laptop img-fluid" alt="">

            <div id="wrapper">
                <h1 class="wrapper__sites">Web Sites</h1>
                <div class="keyF"></div>
                <div class="keyU"></div>
                <div class="keyK"></div>
                <div class="keyC"></div>
            </div>
        </div>
    </header>
    
	<section class="project">
		<div class="container">
			<h1 class="project__title">Think. Design. Create!</h1>
			<p class="project__introduction">Bright style, elegance, refinement, and attention to detail – this is what I value in works of top-level designers. Because of this I always look for new ways to improve my skills and create meticulously designed websites. Although I have not done that many projects within the last couple of years, I sincerely hope this list will grow intensively soon. And yeah, I’m not very good at coding, that is clearly not my thing. But I always find decent solutions and even help my coders with solving their issues:) There is an old Jewish saying: “If you are not allowed to enter through the door, then enter through the window!”</p>
		</div>

		<div class="stack__text">
			<div class="stack__text--projects">My recent</div>
			<div class="stack__text--stack">PROJECTS</div>
		</div>
	</section>
	
	{# end of TEST #}


	{# <div class="container mt-5">
		<div class="row justify-content-start">
			{% for project in projects %}
			{% set auteur = project.users %}
			<div class="col-6 col-lg-3">
				<div class="card m-3">
					<img src="{{ project.mainImage }}" class="card-img-top" alt="{{ project.title }}">
					<div class="card-body">
						<h5 class="card-title">{{ project.title }}</h5>
						<p class="card-text">{{ project.introduction|u.truncate(50, ' ...') }}</p>
						<p class="card-text">Created by {{ auteur }} on {{ project.createdAt }}</p>
						<div class="d-flex justify-content-between">
							{# <a href="{{ path('single_project', {'slug': project.slug }) }}" class="btn__main">Show</a>
							<a href="{{ path('edit_project', {'slug': project.slug }) }}" class="btn__main">Edit</a>
							<a href="{{ path('delete_project', {'slug': project.slug }) }}" class="btn__main">Delete</a> #}
						{# </div>
					</div>
				</div>
			</div>
			{% endfor %}
		</div>
	</div>  #}

	<div class="container">
		<div class="row cards">
			{% for project in projects %}
			{% set auteur = project.users %}
				<div class="col-12 col-lg-6"> 
					<div class="card{{loop.index}}">
						<div class="flips">
							<div class="front{{loop.index}} vertical">
								<div class="card-content--image" style="background-image: url({{ project.mainImage }})"></div>
							</div>
							<div class="back{{loop.index}} vertical">
								<div class="card-content">
									<h2>Back</h2>
									<p>Lorem ipsum</p>
								</div>
							</div>
						</div>
					</div>
				</div>
			{% endfor %}
		</div>
	</div>

	<!-- Footer -->
	{% include "partials/_footer.html.twig" %}
{% endblock %}
